var db = _getDB("com.mysql.jdbc.Driver", "jdbc:mysql://localhost/database?user=user&password=password", "user", "password");
var $rs = db.select("select 1 as ID, abc from table limit 10");
for (var $i=0; $i < $rs.length; $i++){
	_alert($rs[$i]["ID"]+": "+$rs[$i]["abc"]);
}
